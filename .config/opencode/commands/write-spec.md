---
name: write-spec
description: "Write a specification using the spec-writer skill."
---

Use the `spec-writer` skill to create a detailed specification for the plan.

If not plan has been created yet, abort, and ask the user to create a plan first.
